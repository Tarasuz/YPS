import styled from "styled-components";

export const ToolbarWrapper = styled.div`
  display: flex;
  justify-content: space-between;
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
`;
export const IconWrapper = styled.div`
  background: #0e1530;
  border-radius: 100%;
  padding: 4px;
  margin: 3px;
  z-index: 2;
`;

export const FFIcon = styled.i`
  font-size: 1.6rem;
  color: #fadd68;
  margin: 8px;
`;
